name: Build, Test, and Package Reasonix (Windows)

on:
  workflow_dispatch: # Allows you to click "Run workflow" manually

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: main-v2 # Pulls the absolute latest commits from the active branch

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
          check-latest: true

      - name: Run Full Test Suite
        shell: bash
        run: go test ./...

      - name: Run Cache Guard Tests
        shell: bash
        run: |
          export REASONIX_RELEASE_CACHE_GUARD="1"
          go test ./internal/agent

      - name: Build Windows Executable
        shell: bash
        # THE FIX: Pointing to the actual main package directory
        run: go build -o reasonix.exe -v ./cmd/reasonix 

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: reasonix-latest-tested-windows
          path: reasonix.exe
          retention-days: 7
